services:
  melodai:
    build: .
    ports:
      - "${PORT:-5050}:5000"
    env_file:
      - .env
    volumes:
      - melodai-songs:/data/songs
      - melodai-db:/data/db
    restart: unless-stopped

volumes:
  melodai-songs:
  melodai-db:
